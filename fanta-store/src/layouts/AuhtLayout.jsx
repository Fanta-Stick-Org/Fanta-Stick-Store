/* Auth Page */

import React from 'react'

const AuhtLayout = ({ children }) => {
    return (
        <div>
            auth layout
            {children}
        </div>
    )
}

export default AuhtLayout
